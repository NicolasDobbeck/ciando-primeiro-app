import { StyleSheet } from "react-native";

export default StyleSheet.create({
    textoPadrao:{
      color: "red",
      fontSize: 20,
      fontWeight: "bold"
    }
    ,
    container:{
      flex: 1,
      alignItems: "center",
      justifyContent: "center"
    }
})